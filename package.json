{
  "name": "soffle",
  "version": "0.0.1",
  "description": "An arcade RTS space game, based on Solar Scuffle.",
  "main": "index.js",
  "scripts": {
    "serve": "node .",
    "clean": "rm -rf public_build && rm -rf .cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "utrain",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "lo": "^0.41.0",
    "lol": "^1.0.101-lol",
    "parcel": "^2.7.0",
    "parcel-bundler": "^1.12.5",
    "shared": "^0.2.0"
  }
}
